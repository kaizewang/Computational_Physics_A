%% Display Variable Name of Function Input
% Create the following function in a file, |myfun.m|, in your current working folder. 
%
% <include>myfun.m</include>
%
% Call the function at the command prompt using the variables |x| and |y|.
x = 5;
y = 3;
myfun(x,y)
%%
% Call the function using values instead of variables. The |inputname|
% function returns an empty |char| array because its input does not have a
% name.
myfun(5,3)

%% 
% Copyright 2012 The MathWorks, Inc.